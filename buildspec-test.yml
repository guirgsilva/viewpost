version: 0.2

phases:
  pre_build:
    commands:
      - echo "Installing test dependencies..."
      - pip install pytest pytest-cov flask psutil
      
  build:
    commands:
      - echo "Running tests..."
      - cd app
      - python -m pytest -v

artifacts:
  files:
    - '**/*'
  base-directory: '.'